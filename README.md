This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Запуск проекта ( для linux подобных систем )

### 1. Клонирование репозитория

```bash
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
```

Перейдите в папку проекта:

```bash
cd <НАЗВАНИЕ_ПАПКИ_ПРОЕКТА>
```

---

### 2. Проверка порта 3000 (опционально)

По умолчанию Next.js использует порт **3000**. Убедитесь, что он не занят:

```bash
lsof -i :3000
```

Если порт занят, команда вернет список процессов.  
Найдите **PID** (Process ID, например `9012`) и, если процесс вам не нужен, завершите его:

```bash
kill -9 9012
```

⚠️ Если порт 3000 занят и вы не хотите останавливать процесс, Next.js автоматически запустится на следующем свободном порту.

---

### 3. Установка зависимостей

```bash
npm install
```

---

### 4. Запуск приложения

#### Режим разработки (рекомендуется для локальной разработки)

```bash
npm run dev
```

#### Продакшн-режим (с предварительной сборкой)

```bash
npm run build
npm run start
```

---

После запуска приложение будет доступно по адресу:

👉 http://localhost:3000
